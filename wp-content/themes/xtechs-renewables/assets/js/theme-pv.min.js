(function () { 'use strict'; function onReady(fn) { if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', fn); } else { fn(); } } onReady(function () { var pvProcess = document.querySelector('[data-xt-pv-process]'); if (pvProcess) { var stepBtns = pvProcess.querySelectorAll('.xt-pvclone-step-btn'); var panels = pvProcess.querySelectorAll('.xt-pvclone-process-panel'); stepBtns.forEach(function (btn) { btn.addEventListener('click', function () { var id = btn.getAttribute('data-step'); stepBtns.forEach(function (b) { b.classList.toggle('is-active', b === btn); b.setAttribute('aria-selected', b === btn ? 'true' : 'false'); }); panels.forEach(function (p) { var on = p.getAttribute('data-panel') === id; p.toggleAttribute('hidden', !on); p.classList.toggle('is-active', on); }); }); }); } var hubChips = document.querySelector('[data-xt-pv-hub-chips]'); if (hubChips) { hubChips.querySelectorAll('[data-xt-pv-chip]').forEach(function (chip) { chip.addEventListener('click', function () { chip.classList.toggle('is-active'); }); }); } var compare = document.querySelector('[data-xt-pv-compare]'); if (compare) { var track = compare.querySelector('[data-xt-compare-track]'); var left = compare.querySelector('[data-xt-compare-left]'); var right = compare.querySelector('[data-xt-compare-right]'); if (track && left && right) { left.addEventListener('click', function () { track.scrollBy({ left: -300, behavior: 'smooth' }); }); right.addEventListener('click', function () { track.scrollBy({ left: 300, behavior: 'smooth' }); }); } } var faqRoot = document.querySelector('[data-xt-pv-faq]'); if (!faqRoot) { return; } var faqSearch = faqRoot.querySelector('[data-xt-faq-search]'); var faqEmpty = faqRoot.querySelector('[data-xt-faq-empty]'); var faqItems = faqRoot.querySelectorAll('.xt-pvclone-faq-item'); var faqCats = faqRoot.querySelectorAll('.xt-pvclone-faq-nav button[data-faq-cat]'); var activeCat = 'power-backup'; function applyFaqFilter() { var q = faqSearch ? (faqSearch.value || '').trim().toLowerCase() : ''; var n = 0; faqItems.forEach(function (item) { var cat = item.getAttribute('data-faq-cat'); var blob = (item.getAttribute('data-faq-search') || '').toLowerCase(); var catOk = !q && cat === activeCat; var searchOk = q && blob.indexOf(q) !== -1; var show = q ? searchOk : catOk; item.toggleAttribute('hidden', !show); if (show) n += 1; }); if (faqEmpty) { faqEmpty.toggleAttribute('hidden', n !== 0); } } faqCats.forEach(function (btn) { if (btn.tagName !== 'BUTTON') return; btn.addEventListener('click', function () { activeCat = btn.getAttribute('data-faq-cat') || activeCat; faqCats.forEach(function (b) { if (b.tagName === 'BUTTON') { b.classList.toggle('is-active', b === btn); } }); if (faqSearch) faqSearch.value = ''; applyFaqFilter(); }); }); if (faqSearch) { faqSearch.addEventListener('input', function () { applyFaqFilter(); }); } applyFaqFilter(); faqRoot.querySelectorAll('.xt-pvclone-faq-copy').forEach(function (btn) { btn.addEventListener('click', function () { var hash = btn.getAttribute('data-copy-url') || ''; var url = window.location.origin + window.location.pathname + hash; if (navigator.clipboard && navigator.clipboard.writeText) { navigator.clipboard.writeText(url).then(function () { var t = btn.textContent; btn.textContent = 'Copied!'; setTimeout(function () { btn.textContent = t; }, 2000); }); } }); }); }); })();